{
  "name": "@airium/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Airium",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'Linting disabled - TypeScript compilation provides type checking'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "typescript": "^5.0.0"
  }
}